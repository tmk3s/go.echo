# CLAUDE.md

## 言語

**ユーザーへの応答・出力はすべて日本語で書く。** 英語で返さない。

これはコードレビューにも適用される。`/code-review` や `/review` の出力（サマリ、指摘、修正案、PR への
インラインコメント）は日本語で記述すること。サブエージェントを使う場合は、サブエージェントへのプロンプトにも
「最終出力は日本語」と明記して伝播させる。

例外（そのまま英語で書くもの）:

- コード、識別子、ファイルパス、コマンド、ログの引用
- Git のブランチ名
- 技術用語で日本語に訳すと逆に分かりにくいもの（transaction, goroutine, N+1 など）

## プロジェクト構成

`app/` 配下が Go(Echo) のクリーンアーキテクチャ構成、`app/frontend` が Next.js(App Router)。
各層の責務とコマンドは README.md を参照。

## コードレビュー

`.claude/skills/review/SKILL.md` に、専門サブエージェントへ並列でレビューを投げて結果を統合する
`/review` スキルがある。使い方は README.md の「Claude Code コードレビュー」を参照。
